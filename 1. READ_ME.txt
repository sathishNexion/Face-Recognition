1. Install note pad++ for better view of code (OPTIONAL)

2. Install Pycharm IDE 

3. Install following packages using CMD ( code given below, just  COPY-PASTE )
   

        pip install opencv-python
        pip install opencv-contrib-python
        pip install pyvisfile
        pip install numpy
        pip install coverage-python-version
        pip install join
        pip install pytools

4. now copy-past the Codes for Facial Recognation.(steps followed below)
    
      >run Face_samples_collection_code
      >then, run Train_face_samples_code
      >finally, run Final_face_detection_code

       

5. for further Doubt Whats up on : 7598352374

6. Thanks 
